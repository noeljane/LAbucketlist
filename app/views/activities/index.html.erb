<h1>Activities</h1>

<ul>
    <% @activities.each do |a| %>
        <li>
            <%=image_tag(a.image_url) %>
            <%= link_to(a.name, activity_path(a))%>
        </li>
    <% end %>
</ul>

